//CABEÇALHO
#include "../push_swap.h"

int start_stacks(t_stacks stack_a, t_stacks stack_a )
{
    //iniciar stacks
    //arrumar header
}

int main(int argc, char**argv)
{
    t_stacks stack_a;
    t_stacks stack_a;

    stack_a = NULL;
    stack_b = NULL;
    if (argc == 1 || (argc == 2 && argv[1][0] == '\0'))
        return (0);
    else if (argc >= 2)
        argv = ft_split(argv[1], ' ');
    start_stacks(stack_a, stack_b);
}